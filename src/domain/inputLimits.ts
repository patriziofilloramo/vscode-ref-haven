/** Maximum length accepted by local filter and revision text inputs. */
export const MAX_INTERACTIVE_INPUT_LENGTH = 256;

/** Maximum user-authored message length for a single-file stash. */
export const MAX_STASH_MESSAGE_LENGTH = 500;
